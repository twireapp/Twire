<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ============== -->
    <!--      Misc     -->
    <!-- ============== -->
    <attr name="dividerColor" format="color" />
    <attr name="textColor" format="color" />
    <attr name="secondaryTextColor" format="color" />
    <attr name="disabledTextColor" format="color" />
    <attr name="backgroundColor" format="color" />

    <!-- ============== -->
    <!--     Stream     -->
    <!-- ============== -->
    <attr name="streamToolbarColor" format="color" />

    <!-- ============== -->
    <!--      Chat     -->
    <!-- ============== -->
    <attr name="chatSendRippleColor" format="color" />
    <attr name="chatInputBackgroundColor" format="color" />
    <attr name="chatInputTextColor" format="color" />
    <attr name="chatInputHintColor" format="color" />
    <attr name="chatSendIconColor" format="color" />
    <attr name="chatRoomstateIconColor" format="color" />
    <attr name="chatStatusBarColor" format="color" />
    <attr name="chatStatusBarTextColor" format="color" />
    <attr name="chatBackgroundColor" format="color" />

    <!-- ================ -->
    <!--Settings Activity -->
    <!-- ================ -->
    <attr name="settingsBackgroundColor" format="color" />
    <attr name="settingsTextColor" format="color" />
    <attr name="settingsSecondaryTextColor" format="color" />
    <attr name="settingsDisabledTextColor" format="color" />

    <!-- ============== -->
    <!--   MainActivity -->
    <!-- ============== -->
    <attr name="mainActivityIconColor" format="color" />
    <attr name="mainActivityIconTextColor" format="color" />

    <!-- ============== -->
    <!--   Search Activity -->
    <!-- ============== -->
    <attr name="searchActivityUpIconColor" format="color" />
    <attr name="searchActivityCardColor" format="color" />

    <!-- ============== -->
    <!--      Cards     -->
    <!-- ============== -->
    <attr name="mainCardBackgroundColor" format="color" />
    <attr name="cardTextColor" format="color" />

    <!-- ================= -->
    <!-- Navigation drawer -->
    <!-- ================= -->
    <attr name="navigationDrawerBackground" format="color" />
    <attr name="navigationDrawerIconTint" format="color" />
    <attr name="navigationDrawerTextColor" format="color" />
    <attr name="navigationDrawerRippleColor" format="color" />
    <attr name="navigationDrawerHighlighted" format="color" />
</resources>
